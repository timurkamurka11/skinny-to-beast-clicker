# Main Menu BGM

Place the seamless loop file here with the exact name:

`MainMenuBGM.ogg`

The runtime bootstrap loads it from `Resources/Audio/MainMenuBGM`, plays it only in the `MainMenu` scene, enables looping, and follows the existing `settings.music` PlayerPrefs toggle.
